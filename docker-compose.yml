version: '3.4'

services:
  discoveryservice.test:
    image: ${DOCKER_REGISTRY-}discoveryservicetest
    build:
      context: .\test\DiscoveryService.Test
      dockerfile: Dockerfile
      network: dsApi

  discoveryservice.test2:
   image: ${DOCKER_REGISTRY-}discoveryservicetest
   build:
     context: .\test\DiscoveryService.Test
     dockerfile: Dockerfile
     network: dsApi

  discoveryservice.test3:
   image: ${DOCKER_REGISTRY-}discoveryservicetest
   build:
     context: .\test\DiscoveryService.Test
     dockerfile: Dockerfile
     network: dsApi

networks:
  dsApi:
    external:
      name: dsApi